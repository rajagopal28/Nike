{{#paper-dialog class="flex-50" onClose=(action "closeDialog" "cancel") clickOutsideToClose=true}}
  <form>
    {{#paper-toolbar}}
      {{#paper-toolbar-tools}}
        <h2>Confirm action!</h2>
        <span class="flex"></span>
        {{#paper-button iconButton=true onClick=(action "closeDialog" "cancel")}}{{paper-icon icon="close"}}{{/paper-button}}
      {{/paper-toolbar-tools}}
    {{/paper-toolbar}}

    {{#paper-dialog-content}}
        <p>
          {{dialogContent}}
        </p>
    {{/paper-dialog-content}}

    {{#paper-dialog-actions class="layout-row"}}
      <span class="flex"></span>
      {{#paper-button onClick=(action "closeDialog" "ok")}}OK{{/paper-button}}
    {{/paper-dialog-actions}}
  </form>
{{/paper-dialog}}
{{yield}}
