
{{#paper-toolbar}}
  {{#paper-toolbar-tools}}
    {{#paper-button iconButton=true onClick=(action "toggle" leftSideBarLockedOpen)}}
      {{paper-icon "menu"}}
    {{/paper-button}}
    <h2>
      Toolbar with Icon Buttons
    </h2>
    <span class="flex"></span>
    {{#if isAuthenticated}}
      {{#paper-button iconButton=true onClick=(action "logout")}}
        {{paper-icon "power-settings-new"}}
      {{/paper-button}}
    {{/if}}
    {{#paper-button iconButton=true}}
      {{paper-icon "more_vert"}}
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}

{{#paper-sidenav-container class="inner-sidenav"}}

  {{#paper-sidenav
    class="md-whiteframe-z2"
    name="left2"
    open=leftSideBarOpen
    lockedOpen=leftSideBarLockedOpen
    onToggle=(action (mut leftSideBarOpen))}}
    {{#paper-toolbar as |toolbar|}}
      {{#paper-toolbar-tools}}  {{#link-to "labels"}} Home {{/link-to}}{{/paper-toolbar-tools}}
    {{/paper-toolbar}}
    {{#paper-content padding=true}}
      {{#link-to "labels"}} Labels {{/link-to}}
    {{/paper-content}}
    {{#paper-content padding=true}}
      {{#link-to "labels"}} Login {{/link-to}}
    {{/paper-content}}
  {{/paper-sidenav}}
  {{#paper-card-content class="flex"}}
    {{yield}}
  {{/paper-card-content}}
{{/paper-sidenav-container}}
